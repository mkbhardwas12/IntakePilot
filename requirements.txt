# Zero-external-dependency run path (mock LLM + SQLite + local vector index).
fastapi==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
PyYAML==6.0.3
httpx==0.28.1
# Optional (Postgres/pgvector path): pip install asyncpg>=0.30
# Optional (MANAS outbox export, default-off): pip install jsonschema>=4.23
