id: scenario_022
ask: the campaign email workflow is broken, customers get duplicate messages
requester:
  name: Golden User
  dept: Marketing
  role: Ops
scripted_answers:
  urgency: this month
  expected_behavior: each contact gets one email per send
  success_criteria: zero duplicate sends in logs
strict: true
gold:
  questions_asked_max: 7
  slots:
    current_behavior:
      provenance: extracted
    urgency:
      value: this month
  routing_queue: internal-tools
  request_type: bug_report
