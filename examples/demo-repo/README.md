# demo-repo

Target for the 5-minute demo. Confirmed, gate-passing requirements are routed
here as markdown "tickets" (`IPR-*.md`) by the local target plugin
(`core/targets/local.py`). With the GitHub target configured they would become
labeled issues instead.
